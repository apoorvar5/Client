export interface ClubPlayers {
    playerId: number,
    playerName: string,
    playerPos: string,
    playerNationality: string,
}
// TODO complete this and add the same for playerClubs. (everything from scratch)